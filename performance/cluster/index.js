// const cluster = require('cluster');
// const os = require('os');
//
// if (cluster.isMaster) {
//     // cluster.fork();
//     // cluster.fork();
//     // cluster.fork();
//     // cluster.fork();
//     for (let i = 0; i < (os.cpus().length / 2 + 1); i++) {
//         cluster.fork();
//     }
// } else {
//     require('./src');
// }

console.log('NodeJS《第二十八课: cluster 子进程集群》已回顾完毕并通过!如需查看使用,将上述代码的注释剃掉删除即可!');
